

<div >
	<h2 class="exploreHeader">Explore a Random Pokemon</h2>
</div>
<!-- Slideshow container -->
<div class="slideshow-container">

  <!-- Full-width images with number and caption text -->
  <div class="mySlides fade">
		<a href='{{pokemon1.link}}'>
			<img class='slideImage' src="{{pokemon1.image}}" alt="{{pokemon1.name}}">
		</a>
	</div>

	<div class="mySlides fade">
		<a href='{{pokemon2.link}}'>
			<img class='slideImage' src="{{pokemon2.image}}" alt="{{pokemon2.name}}">
		</a>
	</div>

	<div class="mySlides fade">
		<a href='{{pokemon3.link}}'>
			<img class='slideImage' src="{{pokemon3.image}}" alt="{{pokemon3.name}}">
		</a>
	</div>

	<div class="mySlides fade">
		<a href='{{pokemon4.link}}'>
			<img class='slideImage' src="{{pokemon4.image}}" alt="{{pokemon4.name}}">
		</a>
	</div>

	<div class="mySlides fade">
		<a href='{{pokemon5.link}}'>
			<img class='slideImage' src="{{pokemon5.image}}" alt="{{pokemon5.name}}">
		</a>
	</div>

	<div class="mySlides fade">
		<a href='{{pokemon6.link}}'>
			<img class='slideImage' src="{{pokemon6.image}}" alt="{{pokemon6.name}}">
		</a>
	</div>

	<div class="mySlides fade">
		<a href='{{pokemon7.link}}'>
			<img class='slideImage' src="{{pokemon7.image}}" alt="{{pokemon7.name}}">
		</a>
	</div>

	<div class="mySlides fade">
		<a href='{{pokemon8.link}}'>
			<img class='slideImage' src="{{pokemon8.image}}" alt="{{pokemon8.name}}">
		</a>
	</div>

	<div class="mySlides fade">
		<a href='{{pokemon9.link}}'>
			<img class='slideImage' src="{{pokemon9.image}}" alt="{{pokemon9.name}}">
		</a>
	</div>

	<div class="mySlides fade">
		<a href='{{pokemon10.link}}'>
			<img class='slideImage' src="{{pokemon10.image}}" alt="{{pokemon10.name}}">
		</a>
	</div>

  <!-- Next and previous buttons -->
  <a class="prev" onclick="plusSlides(-1)">&#10094;</a>
  <a class="next" onclick="plusSlides(1)">&#10095;</a>
</div>
<br>

<script src="scripts/slideshow.js"></script>
